import { useContext } from "react";
import styled from "styled-components";
import {ProjectContext} from "../../constants/Context";

export default function PurchasePage() {
    return (
        <StyledPage>
            <p>PurchasePage test</p>
        </StyledPage>
    );
}

const StyledPage = styled.div``;
